[build]
  command = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/portal-sophia run build"
  publish = "artifacts/portal-sophia/dist/public"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
